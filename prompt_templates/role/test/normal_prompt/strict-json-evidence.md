# Strict JSON Evidence Contract

Selected normal marker: `V2_TEST_NORMAL_JSON_D4B7`

This reusable fragment defines an output contract, not a work mode. Write the requested
evidence as UTF-8 JSON containing only fields named by the task. Validate the final file
with a real JSON parser. Do not substitute Markdown or prose for the JSON artifact.

