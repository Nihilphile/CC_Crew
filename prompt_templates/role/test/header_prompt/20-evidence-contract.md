Task-header marker: `V2_TEST_HEADER_B_8A46`

Leave machine-readable evidence in the requested temporary artifact. Evidence must
state which markers were observed, which state calls were actually executed, and
which validation commands passed. A claimed action without an observable artifact or
command result is not sufficient.

